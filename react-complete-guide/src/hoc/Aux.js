// higher order components
// used to wrap multiple divs so that we can return 
// multiple jsx elements without the wrapping div

//import React from 'react';

const Aux = props => props.children;

export default Aux;